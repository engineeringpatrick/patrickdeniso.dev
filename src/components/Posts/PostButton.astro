---
const {title, tags, description, href} = Astro.props;
---
<div>
    {tags?.map((tag: string) => {<span class="bg-blue-500 text-white text-sm px-2 py-1 rounded">{tag}</span>})}
    <h3 class="text-xl font-bold mt-2">{title}</h3>
    <p class="text-gray-400 mt-2">
        {description}
    </p>
    <a href={href} class="text-blue-400 hover:underline mt-2 block">Read more →</a>
</div>